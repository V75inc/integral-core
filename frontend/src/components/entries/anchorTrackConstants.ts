/**
 * Sentinel value the entry composer sends for opt-in ``target: track``
 * relations (auto_provision: false). Must match backend
 * ``CREATE_ANCHOR_SENTINEL`` in ``operational_model_entry_fields.py``.
 */
export const CREATE_ANCHOR_SENTINEL = '__create_anchor__';
