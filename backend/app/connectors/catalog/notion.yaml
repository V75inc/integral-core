slug: notion
display_name: Notion
description: Read and update Notion pages, databases, and comments through Notion's hosted MCP server. Requires OAuth into a Notion workspace.
category: mcp_server
kind: mcp
icon: notion
transport: streamable_http
url: https://mcp.notion.com/mcp
auth:
  type: none
  fields: []
# No read_only_tools certification yet, so EVERY tool on this connector stages
# an approval card (ADR-010 §6 default-deny). This server requires OAuth before
# tools/list answers, so the surface could not be transcribed from a live probe
# the way the Google entries were. To lift the gate on reads: connect an
# account, run tools/list, and add the read-only names here — never copy the
# server's readOnlyHint at runtime.
read_only_tools: []
