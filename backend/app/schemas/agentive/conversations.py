"""Schemas for agentive/api/conversations.py.

The conversations CRUD endpoints accept an open ``Dict[str, Any]`` body
(``agent_type``, ``agent_conversation_id``, ``scope``, …) so no Pydantic
models are extracted today. This module exists as a namespace marker for
future typed boundaries.
"""
