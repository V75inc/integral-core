"""Schemas for agentive/api/agent_tools.py.

The tools listing + execution endpoints accept an open ``Dict[str, Any]``
body (parameters + scope passthrough); no Pydantic models needed today.
This module exists as a namespace marker for future request/response shapes.
"""
