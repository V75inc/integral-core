"""Schemas for agentive/api/status.py.

The status endpoint has no request body and returns an open dict (boot-time
introspection of agent registry state). This module exists as a namespace
marker for future typed boundaries.
"""
